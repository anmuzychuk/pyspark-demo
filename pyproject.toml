[project]
name = "pyspark-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "pandas<3.0.0",
    "pyarrow>=18.0.0",
    "pyspark>=4.2.0",
    "seaborn>=0.13.2",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]

[tool.ruff]
line-length = 120
lint.extend-select = ["E", "W", "I", "N", "D", "T20", "ANN"]
lint.ignore = ["D1", "D401"]
